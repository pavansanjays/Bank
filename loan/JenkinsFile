pipeline {

    agent none   // using per-stage agents

    environment {
        APP_JAR = 'target/*.jar'
    }

    stages {
        stage('Build Stage') {
            agent {
                docker {
                    image 'maven:3.9.6-eclipse-temurin-21'
                    // no volume, keep it simple
                    // args ''  // can be omitted
                }
            }
            steps {
                echo "This is the building stage"
                dir('loan') {
                    // Use a writable folder inside the Maven container
                    sh 'mvn -Dmaven.repo.local=/tmp/m2_repo clean install'
                }
            }
        }

        stage('Testing') {
            agent {
                docker {
                    image 'maven:3.9.6-eclipse-temurin-21'
                    // no volume
                }
            }
            steps {
                echo "Running unit tests"
                dir('loan') {
                    sh 'mvn -Dmaven.repo.local=/tmp/m2_repo test'
                }
            }
        }

        stage('Archive jar file') {
            steps {
                echo "This is the archiving file stage"
                dir('loan') {
                    archiveArtifacts artifacts: APP_JAR
                }
            }
        }
    }

    post {
        always {
            echo "always in post"
        }
    }
}
